<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Button color -->
    <solid android:color="@color/mf_primary" />

    <!-- Rounded corners -->
    <corners android:radius="14dp" />

    <!-- Inner padding -->
    <padding
        android:left="16dp"
        android:top="12dp"
        android:right="16dp"
        android:bottom="12dp" />
</shape>
