package com.example.project2;

import java.util.ArrayList;

public class ExpenseStorage {
    // Simple in‑memory list (will reset when app is closed)
    public static ArrayList<Expense> expenses = new ArrayList<>();
}
